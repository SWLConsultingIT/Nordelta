import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // El indicador flotante de desarrollo tapa el pie del riel y ensucia
  // las capturas que se usan para revisar el diseño.
  devIndicators: false,
  /* config options here */
};

export default nextConfig;
